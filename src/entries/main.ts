import { run } from '../alfred-workflow';

const main = async (): Promise<void> => {
  // workflow.sendResult([
  //   createItem({ title: 'Test-1' }).get(),
  //   createItem({ title: 'Test-2' }).get(),
  //   createItem({ title: 'Test-3' }).get(),
  // ]);
}

run(main);
